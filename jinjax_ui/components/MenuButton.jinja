{#def target #}
{#js jxui.js, jxui-menu.js #}

{% do attrs.setdefault(tabindex="0") %}

<button {{ attrs.render(
  type="button",
  data_menu_target=target,
  aria_controls=target,
) }}>{{ content }}</button>
