{#def hidden=False, tag="div" #}

{% do attrs.setdefault(tabindex="0") %}

<{{ tag }} {{ attrs.render(
  data_ui="tabpanel",
  role="tabpanel",
  data_ui_hidden=hidden,
) }}>{{ content }}</{{ tag }}>
